input 1 =< n
output s = (n*(n+1)*(2*n+1)/6)
program {
     	s = 1;  
     	i = 1;  
     	while (i =< (n-1)) {
	     i = i+1; 
	     s = s+(i*i);
             };
	}
.

The lexical analyzer will not read past the '.', so everything here is just a comment.

Construct the correct loop invariant for the above program.  

In writing the loop invariant, you may make use of a function, sigma(n), which is short-hand for 1*1 + 2*2 + ... n*n.
